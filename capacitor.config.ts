import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'candidate_information',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
